<%= simple_form_for @project do |f| %>
	<%= f.input :title %>
	<br>
	<%= f.input :description, :as => :text, :label => false, :input_html => {:class => "redactor"}%>
	<br>
	<%= f.input :misc_html, :as => :text, :label => false, :input_html => {:id => 'redactor', :class => "redactor"}%>
	<br>
	<%= f.button :submit %>
<% end %>
